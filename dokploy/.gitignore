# The onboarding packages carry plaintext secrets for production
# installations: database passwords, encryption keys and each client's
# admin password.
#
# They live here while the onboarding is happening. The good copy goes to
# the password manager, and doesn't leave it from there.
clientes/
!clientes/.gitkeep
